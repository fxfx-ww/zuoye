# 2026-09-09 工作日志

## LLM 概念 wiki 式讲解（可视化模式）

**用户请求**："讲解LLM wiki"——与之前的"生成学习资料"不同，这次是轻量讲解，采用内联可视化（show_widget）而非生成文档文件。

**讲解结构**（4 张可视化 + 文字串联）：
1. AI 家谱嵌套图（AI ⊃ ML ⊃ DL ⊃ Transformer → LLM）+ 时间线 chips（2017 Transformer / 2018 BERT / 2020 GPT-3 / 2022 ChatGPT）
2. 下一个 token 预测 SVG（"今天 天气 真 → ?"概率柱状图，温度参数提示）
3. 三步训练流水线卡片（预训练→SFT→RLHF/DPO）
4. LLM 与已学概念地图（中心 LLM，四卫星：Agent=大脑 / 上下文=视野 / Skill=能力包 / RAG=外接知识）

**核心教学要点**：LLM 本质是"文字接龙机器"——只做下一 token 预测，规模化后涌现推理能力；三大短板（知识过时/无记忆/不会动手）正好对应 RAG/上下文管理/Agent+Skill 的存在意义。

**系列进度**：Agent → 上下文 → Skill → RAG → LLM（地基）。五个概念的知识网已完整。

**方法论**：用户说"讲解"时用内联可视化；说"生成学习资料/调用Skill"时才走 deep-concept-teaching 全流程产出三格式文档。

## 安装 GitHub 能力

**用户请求**："安装Git hub agent"

**执行结果**：
1. 在 BuiltinMarket 搜索 "github"（17 个结果），安装 top 结果 **github** Skill（v1.0.0，管理 Issues/PR/CI，基于 gh CLI 用法文档）到 `~/.workbuddy/skills/github/`
2. 安全审查通过（P2）：仅 SKILL.md 文档 + 图标 + meta，无可执行内容
3. 本机无 gh CLI；用户在 AskUserQuestion 中选择**启用 WorkBuddy 内置 GitHub 连接器**（放弃装 CLI），已指引其到连接器管理页面 OAuth 授权

**状态**：github Skill 已装（但依赖的 gh CLI 未装，暂不可执行实际操作）；GitHub 连接器待用户在 UI 中自行授权连接。
